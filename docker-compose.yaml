version: "3.9"

services:
  mmwave-visualizer:
    build: .
    container_name: mmwave-visualizer
    ports:
      - "${FLASK_PORT:-5000}:${FLASK_PORT:-5000}"
    env_file:
      - .env
    restart: unless-stopped
